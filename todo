# Create tensorflow 1.4 environment
conda create -n tf1 pip python=3.7  # select python version
conda install tensorflow-gpu cudatoolkit=10.0

# install librosa
conda install -c conda-forge librosa 

# add new kernel to jupyter
